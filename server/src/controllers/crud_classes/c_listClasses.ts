import {Request, Response} from 'express';

export default class listClasses {

  async index(req: Request, res: Response) {

    // Requisição dos filtros de pesquisa; 

    // Tipagem dos filtros;

    // Validar se os filtros existem;

    // Filtragem de Aulas;

    // Retornar uma Lista filtrada de aulas; 
  };
};